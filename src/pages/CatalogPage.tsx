import { Button, ProductCatalog } from "../components/index";

const CatalogPage = () => {
  return (
    <div className="container">
      <ProductCatalog />
      <Button />
    </div>
  );
};

export default CatalogPage;
